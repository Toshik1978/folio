# Reject `npm install` when the active Node is outside package.json "engines"
# (the frontend build/test toolchain targets Node 24 LTS; newer Node exposes a
# native localStorage global that shadows happy-dom in tests). Fail loudly at
# install time instead of drifting to a version CI/Docker don't use.
engine-strict=true
